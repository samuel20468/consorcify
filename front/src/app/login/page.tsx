import Login from "@/components/Login/Login";

const page = () => {
    return (
        <div className="flex items-center justify-center h-screen">
            <Login />
        </div>
    );
};

export default page;
