import Register from "@/components/Register/Register";
import React from "react";

const page = () => {
    return (
        <div className="flex">
            <Register />
        </div>
    );
};

export default page;
